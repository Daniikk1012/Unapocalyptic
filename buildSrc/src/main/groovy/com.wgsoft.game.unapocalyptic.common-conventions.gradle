plugins {
    id 'eclipse'
}

repositories {
    mavenCentral()
}

version = '0.2.0'

ext {
    assetDir = '../assets'
}

tasks.withType(JavaCompile) {
    sourceCompatibility = 8
    targetCompatibility = 8
}

tasks.all {
    dependsOn rootProject.texturePacker
}
